//const { default: baseApi } = require('./baseApi');

async function deletePostById(posts, id) {
  // const res = await baseApi.delete(`/posts/${id}`);
    newPosts = posts 
    return res.data;
}

export default deletePostById;